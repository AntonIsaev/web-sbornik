<h1><%= t('devise.instructions.unlock.title', :default => t('en.devise.instructions.unlock.title')) %></h1>

<h3><%= t('devise.instructions.unlock.welcome', :email => @resource.email, :default => t('en.devise.instructions.unlock.welcome', :email => @resource.email)) %></h3>

<p><%= t('devise.instructions.unlock.text', :default => t('en.devise.instructions.unlock.text')) %></p>

<p><%= t('devise.instructions.unlock.text_2', :default => t('en.devise.instructions.unlock.text_2')) %></p>

<p><strong><%= link_to t('devise.instructions.unlock.link', :default => t('en.devise.instructions.unlock.link')), unlock_url(@resource, unlock_token: @token) %></strong></p>

<p><%= t('devise.instructions.footer', :default => t('en.devise.instructions.footer')) %></p>
